[Unit]
Description=Mapit Server

[Service]
ExecStart=$(which docker) run -p 8020:80 nordgedanken/mapit-germany-docker --name "mapit"
ExecStop=$(which docker) stop -t 2 mapit
User=daemon
Group=docker

[Install]
WantedBy=multi-user.target
