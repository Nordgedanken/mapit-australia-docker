[Unit]
Description=Mapit Server
After=docker.target

[Service]
ExecStart=docker run -p 8020:80 -i -t nordgedanken/mapit-germany-docker /bin/bash
ExecStop=docker stop $(docker ps -q --filter ancestor=nordgedanken/mapit-germany-docker)

[Install]
WantedBy=multi-user.target
